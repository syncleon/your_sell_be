spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: "admin"
    password: "root"
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        default_schema: "public"
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 200MB
jwt:
  secret:
    key: "thisissecretkeyformypersonalspringbootapp"
  expiration: 86400000

file:
  upload-dir: /Users/syncleon/app/uploads
